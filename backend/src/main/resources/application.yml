# ?? ?? ? response ? message ????
server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    url: jdbc:mysql://localhost:3300/bbs
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: bbs
    password: jong9612!